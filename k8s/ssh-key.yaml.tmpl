# =============================================================================
# SSH Key Secret - authorized_keys for SSH access
# =============================================================================
# Create this secret with your SSH public key:
#   kubectl create secret generic snry-ssh-key \
#     --from-file=authorized_keys=./authorized_keys \
#     -n snry
# =============================================================================